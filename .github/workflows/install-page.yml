name: Install page

# Checks that the install page keeps its five AI-family setup sections static,
# visible, and complete, so the progressive-enhancement family filter in
# site/js/install.js stays an enhancement. With JavaScript blocked by the site's
# CSP, failed, or disabled, no family section may disappear. See
# tools/check-install-page.py for what it proves and does not prove.

on:
  pull_request:
    paths:
      - "site/install/index.html"
      - "site/styles.css"
      - "site/_headers"
      - "site/js/install.js"
      - "tools/check-install-page.py"
      - ".github/workflows/install-page.yml"
  push:
    branches:
      - main
    paths:
      - "site/install/index.html"
      - "site/styles.css"
      - "site/_headers"
      - "site/js/install.js"
      - "tools/check-install-page.py"
      - ".github/workflows/install-page.yml"

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Check the install page stays static and complete
        shell: bash
        run: python3 tools/check-install-page.py
